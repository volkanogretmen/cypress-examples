//////////////////////////////////////////////////////
// Clear all cookies
/////////////////////////////////////////////////////

cy.clearCookies();
// Note: Cypress automatically clears all cookies before each test.
// Only use this when you need to clear cookies inside a test.


//////////////////////////////////////////////////////
// Set a basic cookie
/////////////////////////////////////////////////////

cy.setCookie('accept', 'true', {secure: true});

//////////////////////////////////////////////////////
// Check if a cookie has a specific value
/////////////////////////////////////////////////////

cy.getCookie('accept')
  .should('have.property', 'value', 'true');


//////////////////////////////////////////////////////
// Check if a cookie exists
/////////////////////////////////////////////////////

cy.getCookie('accept').should('exist').

//////////////////////////////////////////////////////
// Log all contents of cookies in the Cypress log
// We need to using stringify as .getCookies() returns an object
/////////////////////////////////////////////////////

cy.getCookies().then((cookies) => {
            cy.log(JSON.stringify(cookies))
          })
      });
